name: secure_lead_engine
description: A secure lead intelligence engine with PII scrubbing and AI enrichment.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Core Logic
  supabase_flutter: ^2.12.0
  flutter_dotenv: ^6.0.0
  uuid: ^4.0.0

  # Data Handling
  file_picker: ^10.3.10
  csv: ^6.0.0

  # UI & State
  provider: ^6.1.5+1
  fl_chart: ^0.69.0
  go_router: ^14.0.0
  cupertino_icons: ^1.0.2
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
